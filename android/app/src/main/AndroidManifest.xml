<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kyald.jadwalmatkul">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JadwalMatkul.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MatkulDetailActivity"
            android:label="@string/title_detail_detail"
            android:parentActivityName=".MatkulListActivity"
            android:theme="@style/Theme.JadwalMatkul.NoActionBar.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.kyald.jadwalmatkul.MatkulListActivity" />
        </activity>
        <activity
            android:name=".MatkulListActivity"
            android:label="@string/title_matkul_list"
            android:theme="@style/Theme.JadwalMatkul.NoActionBar">

        </activity>
    </application>

</manifest>